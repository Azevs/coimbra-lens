{
  "name": "tinta-na-alta",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "build:nivel": "node scripts/build-nivel.mjs",
    "build:vozes": "powershell -ExecutionPolicy Bypass -File scripts/vozes.ps1",
    "build:mapa": "node scripts/build-mapa.mjs"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
